#zenroom -z credentialIssuerKeygen.zen |jq | tee out/redentialIssuerKeypair.json
Scenario credential: issuer keygen
    Given that I am known as 'Moncon'
    When I create the issuer key
    Then print my 'keys'
