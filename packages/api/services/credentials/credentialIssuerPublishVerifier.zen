#zenroom -k out/redentialIssuerKeypair.json -z credentialIssuerPublishVerifier.zen |jq | tee out/credentialIssuerVerifier.json
#zenroom -a out/redentialIssuerKeypair.json -z credentialIssuerPublishVerifier.zen |jq | tee out/credentialIssuerVerifier.json
Scenario 'credential': Issuer public key generation
Given that I am known as 'Moncon'
and I have my 'keys'
When I create the issuer public key
Then print my 'issuer public key'